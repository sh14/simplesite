<?php
/**
 * User: sh14ru
 * Date: 19.04.18
 */

global $global_actions;
if ( empty( $global_actions ) ) {
	$global_actions = array();
}

global $global_filters;
if ( empty( $global_filters ) ) {
	$global_filters = array();
}

global $scripts;
if ( empty( $scripts ) ) {
	$scripts = array();
}

global $enque_script;
if ( empty( $enque_script ) ) {
	$enque_script = array();
}

global $enque_style;
if ( empty( $enque_style ) ) {
	$enque_style = array();
}


// eof
